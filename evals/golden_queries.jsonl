{"id": "agg-01", "question": "How much did I spend on groceries in March 2026?", "tools": ["sql"], "note": "single category, single month", "expect_refusal": false, "expected_value": -921.75, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='groceries' AND t.type = 'purchase' AND substr(t.posted_date,1,7)='2026-03'"}
{"id": "agg-02", "question": "What was my total spending in January 2026?", "tools": ["sql"], "note": "all purchases in a month", "expect_refusal": false, "expected_value": -4727.56, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t WHERE t.type = 'purchase' AND substr(t.posted_date,1,7)='2026-01'"}
{"id": "agg-03", "question": "How many transactions do I have in total?", "tools": ["sql"], "note": "raw count, all types", "expect_refusal": false, "expected_value": 832, "reference_sql": "SELECT COUNT(*) FROM transactions t"}
{"id": "agg-04", "question": "What was my single largest purchase?", "tools": ["sql"], "note": "largest outflow = most negative", "expect_refusal": false, "expected_value": -1450.0, "reference_sql": "SELECT ROUND(MIN(t.amount),2) FROM transactions t WHERE t.type = 'purchase'"}
{"id": "agg-05", "question": "How much did I spend on coffee over the whole period?", "tools": ["sql"], "note": "category total", "expect_refusal": false, "expected_value": -1103.93, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='coffee' AND t.type = 'purchase'"}
{"id": "agg-06", "question": "How many times did I buy coffee?", "tools": ["sql"], "note": "count not sum", "expect_refusal": false, "expected_value": 154, "reference_sql": "SELECT COUNT(*) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='coffee' AND t.type = 'purchase'"}
{"id": "agg-07", "question": "What is my average grocery transaction?", "tools": ["sql"], "note": "average", "expect_refusal": false, "expected_value": -65.77, "reference_sql": "SELECT ROUND(AVG(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='groceries' AND t.type = 'purchase'"}
{"id": "agg-08", "question": "How much total income did I receive?", "tools": ["sql"], "note": "income is inflow, positive", "expect_refusal": false, "expected_value": 61100.0, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t WHERE t.type='income'"}
{"id": "agg-09", "question": "How much did I pay in bank fees?", "tools": ["sql"], "note": "non-purchase type", "expect_refusal": false, "expected_value": -11.7, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t WHERE t.type='fee'"}
{"id": "agg-10", "question": "How much did I spend on rent in total?", "tools": ["sql"], "note": "large recurring category", "expect_refusal": false, "expected_value": -17400.0, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='rent' AND t.type = 'purchase'"}
{"id": "date-01", "question": "How much did I spend between 2026-01-01 and 2026-03-31?", "tools": ["sql"], "note": "explicit range", "expect_refusal": false, "expected_value": -13739.57, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t WHERE t.type = 'purchase' AND t.posted_date BETWEEN '2026-01-01' AND '2026-03-31'"}
{"id": "date-02", "question": "What did I spend in the last three months of the data?", "tools": ["sql"], "note": "relative window", "expect_refusal": false, "expected_value": -13316.14, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t WHERE t.type = 'purchase' AND t.posted_date >= '2026-05-01'"}
{"id": "date-03", "question": "How much did I spend on dining in the second half of 2025?", "tools": ["sql"], "note": "category + range", "expect_refusal": false, "expected_value": -554.76, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='dining' AND t.type = 'purchase' AND t.posted_date < '2026-01-01'"}
{"id": "date-04", "question": "What is the date of my earliest transaction?", "tools": ["sql"], "note": "date scalar", "expect_refusal": false, "expected_value": "2025-08-01", "reference_sql": "SELECT MIN(t.posted_date) FROM transactions t"}
{"id": "date-05", "question": "What is the date of my most recent transaction?", "tools": ["sql"], "note": "date scalar", "expect_refusal": false, "expected_value": "2026-07-31", "reference_sql": "SELECT MAX(t.posted_date) FROM transactions t"}
{"id": "date-06", "question": "How much did I spend on travel in April 2026?", "tools": ["sql"], "note": "the injected spike month", "expect_refusal": false, "expected_value": -2995.7, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='travel' AND t.type = 'purchase' AND substr(t.posted_date,1,7)='2026-04'"}
{"id": "date-07", "question": "How many months of data do I have?", "tools": ["sql"], "note": "distinct months", "expect_refusal": false, "expected_value": 12, "reference_sql": "SELECT COUNT(DISTINCT substr(t.posted_date,1,7)) FROM transactions t"}
{"id": "date-08", "question": "How much did I spend in December 2025?", "tools": ["sql"], "note": "single month", "expect_refusal": false, "expected_value": -4509.25, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t WHERE t.type = 'purchase' AND substr(t.posted_date,1,7)='2025-12'"}
{"id": "cmp-01", "question": "Did I spend more on groceries or dining overall?", "tools": ["sql"], "note": "two aggregates then compare; answer is a name", "expect_refusal": false, "expected_value": "groceries", "reference_sql": "SELECT c.name FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name IN ('groceries','dining') AND t.type='purchase' GROUP BY c.name ORDER BY SUM(t.amount) ASC LIMIT 1"}
{"id": "cmp-02", "question": "How much more did I spend in April 2026 than in March 2026?", "tools": ["sql"], "note": "difference of two windows", "expect_refusal": false, "expected_value": -2279.54, "reference_sql": "SELECT ROUND( (SELECT SUM(t.amount) FROM transactions t WHERE t.type = 'purchase' AND substr(t.posted_date,1,7)='2026-04') - (SELECT SUM(t.amount) FROM transactions t WHERE t.type = 'purchase' AND substr(t.posted_date,1,7)='2026-03'), 2)"}
{"id": "cmp-03", "question": "Which month had the highest total spending?", "tools": ["sql"], "note": "argmax over months", "expect_refusal": false, "expected_value": "2026-04", "reference_sql": "SELECT substr(t.posted_date,1,7) FROM transactions t WHERE t.type = 'purchase' GROUP BY 1 ORDER BY SUM(t.amount) ASC LIMIT 1"}
{"id": "cmp-04", "question": "Which category do I spend the most on?", "tools": ["sql"], "note": "argmax", "expect_refusal": false, "expected_value": "rent", "reference_sql": "SELECT c.name FROM transactions t JOIN categories c ON c.id=t.category_id WHERE t.type = 'purchase' GROUP BY c.id ORDER BY SUM(t.amount) ASC LIMIT 1"}
{"id": "cmp-05", "question": "Which merchant did I spend the most money at?", "tools": ["sql"], "note": "argmax over merchants", "expect_refusal": false, "expected_value": "Rent", "reference_sql": "SELECT m.canonical_name FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE t.type = 'purchase' GROUP BY m.id ORDER BY SUM(t.amount) ASC LIMIT 1"}
{"id": "cmp-06", "question": "Which merchant do I visit most often?", "tools": ["sql"], "note": "frequency not value \u2014 a classic confusion", "expect_refusal": false, "expected_value": "Blue Bottle Coffee", "reference_sql": "SELECT m.canonical_name FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE t.type = 'purchase' GROUP BY m.id ORDER BY COUNT(*) DESC LIMIT 1"}
{"id": "cmp-07", "question": "Was my spending in the first six months higher than the last six?", "tools": ["sql"], "note": "boolean comparison", "expect_refusal": false, "expected_value": "no", "reference_sql": "SELECT CASE WHEN (SELECT SUM(t.amount) FROM transactions t WHERE t.type = 'purchase' AND t.posted_date < '2026-02-01') < (SELECT SUM(t.amount) FROM transactions t WHERE t.type = 'purchase' AND t.posted_date >= '2026-02-01') THEN 'yes' ELSE 'no' END"}
{"id": "cmp-08", "question": "How much less did I spend on dining than on groceries?", "tools": ["sql"], "note": "signed difference", "expect_refusal": false, "expected_value": 8305.29, "reference_sql": "SELECT ROUND( (SELECT SUM(t.amount) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='dining' AND t.type='purchase') - (SELECT SUM(t.amount) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='groceries' AND t.type='purchase'), 2)"}
{"id": "mer-01", "question": "How much did I spend at Amazon?", "tools": ["sql"], "note": "the merchant that was split before the tier-3 fix", "expect_refusal": false, "expected_value": -7356.74, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Amazon%' AND t.type = 'purchase'"}
{"id": "mer-02", "question": "How many times did I shop at Target?", "tools": ["sql"], "note": "merchant count", "expect_refusal": false, "expected_value": 44, "reference_sql": "SELECT COUNT(*) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Target%' AND t.type = 'purchase'"}
{"id": "mer-03", "question": "How much do I pay Netflix each month?", "tools": ["sql"], "note": "reads recurring_series, not raw transactions", "expect_refusal": false, "expected_value": -15.49, "reference_sql": "SELECT ROUND(s.typical_amount,2) FROM recurring_series s JOIN merchants m ON m.id=s.merchant_id WHERE m.canonical_name LIKE 'Netflix%'"}
{"id": "mer-04", "question": "How many distinct merchants have I bought from?", "tools": ["sql"], "note": "distinct count; 'bought from' excludes payroll and transfers", "expect_refusal": false, "expected_value": 19, "reference_sql": "SELECT COUNT(DISTINCT t.merchant_id) FROM transactions t WHERE t.merchant_id IS NOT NULL AND t.type = 'purchase'"}
{"id": "mer-05", "question": "What did I spend at Whole Foods?", "tools": ["sql"], "note": "abbreviation-resolved merchant", "expect_refusal": false, "expected_value": -4153.63, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Whole Foods%' AND t.type = 'purchase'"}
{"id": "mer-06", "question": "How much did I spend on Uber rides?", "tools": ["sql"], "note": "merchant total", "expect_refusal": false, "expected_value": -2329.07, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Uber%' AND t.type = 'purchase'"}
{"id": "rec-01", "question": "How many recurring charges do I have?", "tools": ["sql"], "note": "recurring_series count", "expect_refusal": false, "expected_value": 8, "reference_sql": "SELECT COUNT(*) FROM recurring_series s WHERE s.active=1"}
{"id": "rec-02", "question": "Did any of my subscriptions go up in price?", "tools": ["anomaly", "sql"], "note": "the injected Spotify hike", "expect_refusal": false, "expected_value": "Spotify", "reference_sql": "SELECT m.canonical_name FROM recurring_series s JOIN merchants m ON m.id=s.merchant_id WHERE ABS(s.last_amount) > ABS(s.typical_amount)*1.05 LIMIT 1"}
{"id": "rec-03", "question": "What is my most expensive recurring charge?", "tools": ["sql"], "note": "argmax over series", "expect_refusal": false, "expected_value": "Rent", "reference_sql": "SELECT m.canonical_name FROM recurring_series s JOIN merchants m ON m.id=s.merchant_id WHERE s.typical_amount < 0 ORDER BY s.typical_amount ASC LIMIT 1"}
{"id": "rec-04", "question": "How much do my subscriptions cost me per month in total?", "tools": ["sql"], "note": "sum over a filtered series set", "expect_refusal": false, "expected_value": -50.47, "reference_sql": "SELECT ROUND(SUM(s.typical_amount),2) FROM recurring_series s JOIN merchants m ON m.id=s.merchant_id JOIN categories c ON c.id=m.default_category_id WHERE s.active=1 AND c.name='subscriptions'"}
{"id": "rec-05", "question": "Is my travel spending unusual in any month?", "tools": ["anomaly"], "note": "the injected z=16 spike", "expect_refusal": false, "expected_value": "2026-04", "reference_sql": "SELECT substr(t.posted_date,1,7) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='travel' AND t.type = 'purchase' GROUP BY 1 ORDER BY SUM(ABS(t.amount)) DESC LIMIT 1"}
{"id": "rec-06", "question": "When did I first shop at Peloton?", "tools": ["sql"], "note": "new merchant, final month only", "expect_refusal": false, "expected_value": "2026-07-12", "reference_sql": "SELECT MIN(t.posted_date) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Peloton%'"}
{"id": "multi-01", "question": "What percentage of my spending was on essentials?", "tools": ["sql"], "note": "ratio of two aggregates", "expect_refusal": false, "expected_value": 53.5, "reference_sql": "SELECT ROUND(100.0 * (SELECT SUM(t.amount) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.is_essential=1 AND t.type = 'purchase') / (SELECT SUM(t.amount) FROM transactions t WHERE t.type = 'purchase'), 1)"}
{"id": "multi-02", "question": "In my highest-spending month, which category dominated?", "tools": ["sql"], "note": "argmax feeding a second argmax", "expect_refusal": false, "expected_value": "travel", "reference_sql": "SELECT c.name FROM transactions t JOIN categories c ON c.id=t.category_id WHERE t.type = 'purchase' AND substr(t.posted_date,1,7) = ( SELECT substr(t2.posted_date,1,7) FROM transactions t2 WHERE t2.type='purchase' GROUP BY 1 ORDER BY SUM(t2.amount) ASC LIMIT 1) GROUP BY c.id ORDER BY SUM(t.amount) ASC LIMIT 1"}
{"id": "multi-03", "question": "What is my average monthly spending?", "tools": ["sql"], "note": "aggregate of an aggregate", "expect_refusal": false, "expected_value": -4664.06, "reference_sql": "SELECT ROUND(AVG(monthly),2) FROM ( SELECT SUM(t.amount) AS monthly FROM transactions t WHERE t.type = 'purchase' GROUP BY substr(t.posted_date,1,7))"}
{"id": "multi-04", "question": "How much did I save, counting income minus spending?", "tools": ["sql"], "note": "mixed types, sign-aware", "expect_refusal": false, "expected_value": 5119.53, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t WHERE t.type IN ('income','purchase','fee')"}
{"id": "multi-05", "question": "What percentage of my grocery spending went to Trader Joe's?", "tools": ["sql"], "note": "merchant share of category", "expect_refusal": false, "expected_value": 57.3, "reference_sql": "SELECT ROUND(100.0 * (SELECT SUM(t.amount) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Trader Joe%' AND t.type='purchase') / (SELECT SUM(t.amount) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='groceries' AND t.type='purchase'), 1)"}
{"id": "sem-01", "question": "What did I spend on anything medical-looking?", "tools": ["semantic", "sql"], "note": "fuzzy phrasing, health category", "expect_refusal": false, "expected_value": -1874.81, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='health' AND t.type = 'purchase'"}
{"id": "sem-02", "question": "How much went on entertainment and going out?", "tools": ["semantic", "sql"], "note": "spans two categories", "expect_refusal": false, "expected_value": -2020.16, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name IN ('entertainment','dining') AND t.type = 'purchase'"}
{"id": "sem-03", "question": "What did my trips cost me?", "tools": ["semantic", "sql"], "note": "colloquial phrasing", "expect_refusal": false, "expected_value": -7586.88, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN categories c ON c.id=t.category_id WHERE c.name='travel' AND t.type = 'purchase'"}
{"id": "sem-04", "question": "What do I pay for my gym membership?", "tools": ["semantic", "sql"], "note": "13 rows; 'gym' appears in no field \u2014 descriptor reads PLANET FIT", "expect_refusal": false, "expected_value": -324.87, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Planet Fit%' AND t.type = 'purchase'"}
{"id": "sem-05", "question": "How much did that hotel stay in Austin cost me?", "tools": ["semantic", "sql"], "note": "8 rows; the city is only in raw_descriptor, so recall must read it", "expect_refusal": false, "expected_value": -1756.72, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Marriott%' AND t.type = 'purchase'"}
{"id": "sem-06", "question": "How much have I spent on flights?", "tools": ["semantic", "sql"], "note": "13 rows; category is 'travel', which also holds hotels \u2014 'flights' is narrower", "expect_refusal": false, "expected_value": -5830.16, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Delta%' AND t.type = 'purchase'"}
{"id": "sem-07", "question": "What is my creative software subscription costing me?", "tools": ["semantic", "sql"], "note": "12 rows; one of several merchants inside 'subscriptions'", "expect_refusal": false, "expected_value": -275.88, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Adobe%' AND t.type = 'purchase'"}
{"id": "sem-08", "question": "How much do I pay for internet and TV?", "tools": ["semantic", "sql"], "note": "12 rows; descriptor is COMCAST *XFINITY, neither word in the question", "expect_refusal": false, "expected_value": -948.0, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Comcast%' AND t.type = 'purchase'"}
{"id": "sem-09", "question": "What did that exercise bike cost?", "tools": ["semantic", "sql"], "note": "a single row \u2014 the hardest possible retrieval target", "expect_refusal": false, "expected_value": -44.0, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'Peloton%' AND t.type = 'purchase'"}
{"id": "sem-10", "question": "How much have I lost to cash machine charges?", "tools": ["semantic", "sql"], "note": "3 rows, and type='fee' \u2014 a purchase filter here returns nothing", "expect_refusal": false, "expected_value": -8.84, "reference_sql": "SELECT ROUND(SUM(t.amount),2) FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE m.canonical_name LIKE 'ATM Fee%'"}
{"id": "anom-01", "question": "Which category had the most unusual month?", "tools": ["anomaly"], "note": "the top-ranked finding by severity", "expect_refusal": false, "expected_value": "travel", "reference_fn": "top_anomaly_category"}
{"id": "anom-02", "question": "In which month did my travel spending look abnormal?", "tools": ["anomaly"], "note": "period of the top finding \u2014 the injected spike", "expect_refusal": false, "expected_value": "2026-04", "reference_fn": "top_anomaly_period"}
{"id": "anom-03", "question": "How much did I actually spend in that abnormal travel month?", "tools": ["anomaly"], "note": "observed magnitude, positive \u2014 must survive to the answer", "expect_refusal": false, "expected_value": 2995.7, "reference_fn": "top_anomaly_observed"}
{"id": "anom-04", "question": "What is my normal monthly travel spend, ignoring the spike?", "tools": ["anomaly"], "note": "the leave-one-out baseline; a plain AVG would differ", "expect_refusal": false, "expected_value": 535.44, "reference_fn": "top_anomaly_baseline"}
{"id": "anom-05", "question": "Did my spending drop unusually low in any category?", "tools": ["anomaly"], "note": "direction='below' \u2014 decreases are anomalies too", "expect_refusal": false, "expected_value": "groceries", "reference_fn": "top_decline_category"}
{"id": "anom-06", "question": "How many category-months look statistically unusual?", "tools": ["anomaly"], "note": "a count over findings; the answer node renders 5 and computes nothing", "expect_refusal": false, "expected_value": 15, "reference_fn": "anomaly_count"}
{"id": "refuse-01", "question": "What will I spend next month?", "tools": [], "note": "no forecasting capability; must refuse rather than extrapolate", "expect_refusal": true, "expected_value": null}
{"id": "refuse-02", "question": "What is my credit score?", "tools": [], "note": "not derivable from statements", "expect_refusal": true, "expected_value": null}
{"id": "refuse-03", "question": "What is my current account balance?", "tools": [], "note": "no balance column; statements record flows, not position", "expect_refusal": true, "expected_value": null}
{"id": "refuse-04", "question": "Should I invest in index funds?", "tools": [], "note": "financial advice \u2014 explicitly out of scope per \u00a71", "expect_refusal": true, "expected_value": null}
{"id": "refuse-05", "question": "How much did I spend on groceries in 2019?", "tools": [], "note": "outside the retrieved date range; must not answer 0", "expect_refusal": true, "expected_value": null}
{"id": "refuse-06", "question": "What is my employer's annual revenue?", "tools": [], "note": "not in the data at all", "expect_refusal": true, "expected_value": null}
{"id": "refuse-07", "question": "Am I on track to retire comfortably?", "tools": [], "note": "advisory, and unanswerable from 12 months of flows", "expect_refusal": true, "expected_value": null}
